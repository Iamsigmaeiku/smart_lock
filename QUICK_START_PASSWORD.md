# 快速開始：觸控密碼功能

## ✅ 編譯成功

```
RAM:   13.7% (44,772 / 327,680 bytes)
Flash: 58.9% (772,397 / 1,310,720 bytes)
```

## 上傳到ESP32

```bash
pio run -t upload
```

## 首次使用

### 1. 接線檢查

**觸控接腳**:
- T_CS (觸控片選) → GPIO15
- T_IRQ (觸控中斷) → GPIO2
- T_DIN → MOSI (GPIO23) - 與螢幕共用
- T_DO → MISO (GPIO19) - 與螢幕共用
- T_CLK → SCK (GPIO18) - 與螢幕共用
- VCC → 3.3V
- GND → GND

### 2. 啟動流程

1. **歡迎畫面** (2秒)
2. **主選單** - 4個按鈕：
   - 🖐️ 指紋辨識 (左上)
   - 💳 RFID卡片 (右上)
   - 🔢 輸入密碼 (左下)
   - 👤 人臉辨識 (右下)

### 3. 使用密碼解鎖

1. 點擊 **"Password"** 按鈕
2. 輸入密碼（預設: `1234`）
3. 按 `*` 確認
4. 成功 → 綠色畫面 → 開鎖5秒
5. 失敗 → 紅色畫面 → 重試（最多3次）

### 4. 數字鍵盤

```
[1] [2] [3]
[4] [5] [6]
[7] [8] [9]
[#] [0] [*]
```

- **數字鍵 (0-9)**: 輸入密碼
- **# 鍵**: 清除密碼
- **\* 鍵**: 確認提交

## 觸控校準（如果觸摸不準）

如果發現觸摸位置不準確：

```bash
# 1. 備份主程式
cp src/main.cpp src/main.cpp.backup

# 2. 使用校準程式
cp test/touch_calibration_test.cpp src/main.cpp

# 3. 編譯上傳
pio run -t upload

# 4. 打開序列埠
pio device monitor

# 5. 觸摸螢幕上的標記點，記錄座標

# 6. 根據輸出調整 include/config.h 中的參數：
#    TS_MINX, TS_MAXX, TS_MINY, TS_MAXY

# 7. 恢復主程式
cp src/main.cpp.backup src/main.cpp
pio run -t upload
```

詳細步驟見 `test/CALIBRATION_GUIDE.md`

## 修改密碼

目前需要修改代碼：

```cpp
// 在 setup() 或新建設定模式中
pwManager.changePassword("1234", "新密碼");
```

未來可以添加長按進入設定模式的功能。

## 安全特性

- ✅ 錯誤3次自動鎖定30秒
- ✅ 密碼以星號 (*) 遮罩顯示
- ✅ 密碼持久化存儲 (重啟不丟失)
- ✅ 支持4-8位數字密碼

## 故障排除

### 觸摸無反應
- 檢查T_CS和T_IRQ接線
- 檢查序列埠是否顯示 "初始化觸控完成"
- 使用校準測試程式檢測

### 位置偏移
- 執行觸控校準
- 調整config.h參數

### 密碼無法保存
- 檢查序列埠錯誤訊息
- 使用 `pwManager.resetToDefault()` 重置

### 按鍵重複觸發
- 增加防抖延遲 (main.cpp 中調整 delay())

## 序列埠輸出

```
================================
智慧門鎖系統啟動中...
================================
初始化指紋感測器完成
初始化螢幕完成
初始化觸控完成
初始化馬達完成
初始化RFID完成
初始化AI鏡頭完成
初始化WiFi完成
密碼管理模組初始化完成
系統準備就緒！
顯示：主選單
```

## 下一步

1. ✅ 測試所有4種驗證方式
2. ✅ 校準觸控精確度
3. ✅ 修改預設密碼
4. ⏳ 添加密碼修改介面
5. ⏳ 添加日誌記錄功能

## 文檔

- 📄 `TOUCHSCREEN_IMPLEMENTATION.md` - 完整實施文檔
- 📄 `test/CALIBRATION_GUIDE.md` - 觸控校準指南
- 📄 `README.md` - 專案總覽

## 技術支持

如有問題，檢查：
1. 序列埠輸出 (115200 baud)
2. 接線是否正確
3. 觸控校準是否完成
4. 密碼是否正確 (預設: 1234)

---

🎉 **恭喜！觸控密碼功能已完整實施並編譯成功！**
